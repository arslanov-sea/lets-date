# lets-date

A small single-page site that asks a question, lets the answer pick a date, time, and ends with a celebration.

Static site, no build step. Open `index.html` or serve the folder with any static file server.
